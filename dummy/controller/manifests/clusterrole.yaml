kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: dummy-controller-role
rules:
  - apiGroups:
      - ""
    resources:
      - pods
    verbs:
      - get
      - list
      - watch
      - create
      - delete
      - patch
      - update
  - apiGroups:
      - stable.dwk
    resources:
      - dummysites
    verbs:
      - get
      - list
      - watch
      - create
      - delete
      - patch
      - update
